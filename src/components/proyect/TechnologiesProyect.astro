<ul class="flex flex-wrap gap-2 flex-row items-center">
  {
    Astro.props.technologies.map((tag: any) => (
      <li>
        <span
          class={`flex gap-x-2 rounded-full text-xs ${tag.class} py-1.5 px-3 border border-white/10 shadow-md`}
        >
          <tag.icon class="w-4 h-4" />
          {tag.name}
        </span>
      </li>
    ))
  }
</ul>
